[pytest]
testpaths = tests
addopts = -ra
markers =
    unit: fast deterministic tests
    integration: tests hitting real HTTP/MCP surfaces
    e2e: browser-driven Playwright tests
    rag: requires embedding model + chroma index ready
    slow: long-running or heavy-network tests
    asyncio: async tests (pytest-asyncio)
